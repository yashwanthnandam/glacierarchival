# Optimized production Dockerfile - uses pre-built files from GitHub Actions
FROM nginx:alpine

# Copy pre-built files from the repository (built in GitHub Actions)
COPY dist /usr/share/nginx/html

# Copy custom nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]