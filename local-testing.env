# Local Testing Environment Configuration
SECRET_KEY=local-test-secret-key-for-development-only
ENCRYPTION_KEY=local-test-encryption-key-for-development-only
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database (using SQLite for local testing)
DATABASE_URL=sqlite:///db_local.sqlite3

# AWS S3 Configuration for Testing
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_STORAGE_BUCKET_NAME=datahibernate-test-local-1760515615
AWS_S3_REGION_NAME=ap-south-1

# Redis (using local Redis or in-memory cache)
REDIS_URL=redis://localhost:6379/1

# Email (disabled for local testing)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Local Test <test@localhost>

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Razorpay (disabled for local testing)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
